# Docker Environment Template for Wazuh MCP Server
# Copy this file to .env and customize for your environment

# =============================================================================
# REQUIRED: WAZUH SERVER CONFIGURATION
# =============================================================================

# Your Wazuh server details
WAZUH_HOST=your-wazuh-server.com
WAZUH_PORT=55000
WAZUH_USER=your-api-user
WAZUH_PASS=your-secure-password

# =============================================================================
# WAZUH INDEXER CONFIGURATION (for distributed deployments)
# =============================================================================

# Wazuh Indexer hostname (if different from Wazuh server)
# Leave commented to use same as WAZUH_HOST
# WAZUH_INDEXER_HOST=your-indexer-server.com

# Wazuh Indexer port (default: 9200)
WAZUH_INDEXER_PORT=9200

# Indexer credentials (if different from Wazuh server)
# Leave commented to use same as WAZUH_USER/WAZUH_PASS
# WAZUH_INDEXER_USER=indexer-username
# WAZUH_INDEXER_PASS=indexer-password

# Enable Indexer features (recommended for Wazuh 4.8.0+)
USE_INDEXER_FOR_ALERTS=true
USE_INDEXER_FOR_VULNERABILITIES=true

# =============================================================================
# MCP TRANSPORT MODE
# =============================================================================

# Choose transport mode:
# - stdio: For Claude Desktop integration (default)
# - http: For remote HTTP/SSE access
MCP_TRANSPORT=stdio

# HTTP transport settings (only used when MCP_TRANSPORT=http)
MCP_HOST=0.0.0.0
MCP_PORT=3000

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Enable SSL certificate verification (recommended: true)
VERIFY_SSL=true

# =============================================================================
# OPTIONAL: PERFORMANCE TUNING
# =============================================================================

# Maximum alerts to retrieve per query
MAX_ALERTS_PER_QUERY=1000

# Request timeout in seconds
REQUEST_TIMEOUT_SECONDS=30

# =============================================================================
# OPTIONAL: EXTERNAL API KEYS (for enhanced threat intelligence)
# =============================================================================

# VirusTotal API key (uncomment to enable)
# VIRUSTOTAL_API_KEY=your-virustotal-api-key

# Shodan API key (uncomment to enable)
# SHODAN_API_KEY=your-shodan-api-key

# AbuseIPDB API key (uncomment to enable)
# ABUSEIPDB_API_KEY=your-abuseipdb-api-key

# =============================================================================
# DOCKER BUILD SETTINGS (optional)
# =============================================================================

# Build timestamp (auto-generated if not set)
# BUILD_DATE=2024-01-01T00:00:00Z